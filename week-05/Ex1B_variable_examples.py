# ╔══════════════════════════════════════════╗
# ║            KIDUS TESEMA                  ║
# ║      ~ Year Up United Student ~          ║
#  ║       Data Analytics Track             ║
# ║                                          ║
# ║     "Turning data into decisions"        ║
# ╚══════════════════════════════════════════╝


# -------------------------------
# Exercise 1B - Variable Examples
# -------------------------------

# Customer information variables
customer_id = 1001  # assumed numeric ID used in database lookup
customer_name = "John Doe"  # full name assumed as single string, but could be split into first/last
customer_gender = "Male"  # assumed categorical value (Male/Female/Other)
customer_dob = "1995-08-15"  # stored as string; could also be datetime object for analysis
driver_license_number = "dgfhg343445654"  # empty string because value may not be available yet
auto_policy_number = 0000AAS3054  # numeric placeholder assuming policy numbers are numeric identifiers

# -------------------------------
# Personal information variables
# -------------------------------
my_name = "kidus tesema"  # could also be split into first_name and last_name
birth_location = "Addis Ababa, Ethiopia"  # combined city and state/country for simplicity


# -------------------------------
# Lab 2 - Reserved words (comments only)
# -------------------------------

# a) Reserved words in Python include:
# False, None, True, and, as, assert, async, await, break, class, continue, def,
# del, elif, else, except, finally, for, from, global, if, import, in, is, lambda,
# nonlocal, not, or, pass, raise, return, try, while, with, yield

# b) Five reserved words and explanations:

# def → used to define a function in Python
# if → used for conditional decision making
# for → used for looping over sequences
# class → used to define object-oriented structures (classes)
# return → used to send a result back from a function
